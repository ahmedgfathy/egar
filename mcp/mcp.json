{
  "name": "EGAR VTiger Private MCP Server",
  "description": "Private MCP server for EGAR VTiger CRM project. Provides file and MariaDB access for AI assistants (localhost only).",
  "version": "1.0.0",
  "entry": "build/index.js",
  "private": true,
  "capabilities": {
    "tools": ["list_files", "read_file", "query_db"]
  },
  "tools": [
    {
      "name": "list_files",
      "description": "List files and directories in the VTiger project.",
      "parameters": {
        "dir": {
          "type": "string",
          "description": "Directory path (relative to VTiger project root)",
          "default": "."
        }
      }
    },
    {
      "name": "read_file",
      "description": "Read the contents of a file in the VTiger project.",
      "parameters": {
        "file": {
          "type": "string", 
          "description": "File path (relative to VTiger project root)"
        }
      }
    },
    {
      "name": "query_db",
      "description": "Execute SQL queries on the VTiger MariaDB 'egar' database.",
      "parameters": {
        "sql": {
          "type": "string",
          "description": "SQL query to execute on the egar database"
        }
      }
    }
  ],
  "security": {
    "access": "localhost-only",
    "database": "egar (MariaDB)",
    "project_path": "/var/www/html/egar"
  }
}
